TF2GAP8 readme file

To run the TensorFlow bridge TF2GAP8 examples requires TensorFlow 1.4 to be installed.

Please run the following command in this directory with the command:

make install

After this has been run and you can execute the examples by executing the following command in the example path:

make clean all run
